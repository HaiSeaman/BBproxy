# 版本变更记录

本文件记录 BBproxy 的版本变更。1.3.0 之前的历史未在此归档。
技术细节与设计原因见 [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md)。

---

## [1.3.0]

本次版本的核心是**修掉会导致"配置丢失"和"代理静默失效"的一批问题**，并把 PAC 判定性能提升约 17 倍。

### 修复

- **白名单同尾域名误判**：白名单里写 `example.com` 时，`notexample.com`、`mygithub.com`
  这类"尾巴相同"的域名会被错误直连（本该走代理）。现在只匹配域名自身与其子域。
- **面板会覆盖你自己的配置**：面板打开后若立刻点击模式卡片，会把默认的
  `127.0.0.1:10808` 和默认白名单整份写回，**覆盖掉你真实的服务器地址与规则**，
  而界面还显示"已保存"。现已从根上消除该竞态。
- **刚输入的白名单规则会丢失**：修改白名单后短时间内又改动主机或端口，
  新加的规则会静默丢失。现已保证任何保存都不漏掉当前输入。
- **HTTPS 代理不可用**：选择 HTTPS 协议时被当作明文 HTTP 代理去连接，代理直接失效。
  现已按 PAC 规范使用 `HTTPS` 标识符。
- **主机名不合法时无提示**：填了含空格的地址（如 `my proxy`）会被静默改成 `127.0.0.1`，
  面板显示一个地址、实际用另一个。现在会直接提示"服务器地址不合法，未保存"。
- **规则大小写**：白名单里写大写（如 `Example.COM`）原本永远匹配不上，现已不区分大小写。
- **带尾点的写法**：`example.com.` 这种 FQDN 写法原本命中不了白名单，现已支持。
- **静默失败**：代理切换超时或失败时，原本只在后台打日志；现在会在面板上明确提示。
- **致命错误被忽略**：代理报致命错误时会被限流丢掉的提示，现已单独标注。

### 性能

- **PAC 判定提速约 17 倍**：重构了白名单的匹配方式（哈希表 + 预编译正则）。
  实测 200 条规则下每次网络请求的判定耗时从 `257 微秒` 降到 `15 微秒`。
  更重要的是：**现在规则条数几乎不影响速度**（50 条与 1 条相差不足 0.1 微秒），
  所以你可以放心把白名单写长。
- 面板打开时的存储读取由两次合并为一次。

### 新增

- **代理故障转移开关**（Auto 模式，默认关闭）：开启后，代理不可达时自动退回直连，
  避免整机断网。开启后代理故障期间的流量会绕过代理，请自行权衡。
- **两种模式的白名单语义对齐**：Global 与 Auto 模式下同一份白名单的分流结果保持一致，
  切换模式不会再让行为悄悄变化。
- 面板新增代理错误提示条，可手动关闭。

### 变更

- 图标改用 PNG（16/32/48/128）。浏览器不支持 SVG 作为扩展图标，此前工具栏图标不显示。
- 工具栏悬停文字修正为「BBproxy - 极简代理切换」（此前配置写了但不生效）。

### 文档

- 新增本文件与 [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md)（开发者文档）。
- README 补齐白名单语义说明，并注明：`192.168.0.0/16` 这类 CIDR 写法仅 Global 模式支持，
  Auto 模式请使用 `192.168.*`。
- 修正 README 中"绝不引发网页断网"的表述——故障转移默认是关闭的。

### 测试

- 单元测试由 36 个增至 67 个。
- 测试基准从"近似模拟"改为**逐字复刻 Chromium 真实实现**，
  修掉了此前"测试全绿但真机有 bug"的假象。
